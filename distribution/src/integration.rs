/// Issue a mining reward to the miner's account.
pub fn issue_reward(miner_address: &str, reward: u64) {
    // TODO: Implement logic to issue rewards via smart contract or direct blockchain logic.
    println!("Issued {} coins to miner {}", reward, miner_address);
}